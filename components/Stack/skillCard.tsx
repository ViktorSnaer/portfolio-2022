import React from "react";

const skillCard = () => {
  return (
    // <div>
    //   <h3>{props.title}</h3>
    //   <ul>
    //     {props.data.map((item) => {
    //       return <li>{item}</li>;
    //     })}
    //   </ul>
    // </div>
  );
};

export default skillCard;
